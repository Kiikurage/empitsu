#[derive(Debug, PartialEq)]
pub enum PunctuatorKind {
    Plus,
    Minus,
    Multiply,
    Divide,
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    SemiColon,
    Comma,
    Equal,
}
